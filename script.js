document.getElementById('btn').addEventListener('click', onButton)

function onButton () {
  alert(document.getElementById('txt').value) // Displays a message that matches the text in the input box.
}
